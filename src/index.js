import React from "react";
import ReactDOM from "react-dom/client";
import NebulaOneROICalculator from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <NebulaOneROICalculator />
  </React.StrictMode>
);
